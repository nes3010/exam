type Task @model @auth(rules: [{allow: private}]) {
  id: ID!
  title: String!
  description: String
  status: String
}

type Note @model @auth(rules: [{allow: private}]) {
  id: ID!
  content: String!
}
 